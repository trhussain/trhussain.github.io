{
  "name": "trhussain.github.io",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "description": "npm run deploy \r - pushes to gh-pages dir",
  "dependencies": {
    "gh-pages": "^6.2.0"
  }
}
